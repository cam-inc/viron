{
  "name": "@viron/linter",
  "version": "0.0.2",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "prebuild": "npm run clean",
    "build": "tsc",
    "clean": "rm -rf dist && rm -f tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "ajv": "^6.12.6"
  },
  "devDependencies": {
    "typescript": "^4.2.4"
  },
  "engines": {
    "node": ">=15.13.0",
    "npm": ">=7.9.0"
  }
}
